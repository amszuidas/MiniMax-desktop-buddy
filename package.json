{
  "name": "MiniMax-desktop-buddy",
  "private": true,
  "license": "MIT",
  "type": "module",
  "engines": { "node": ">=20" },
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "bridge": "pnpm --filter @buddy/bridge bridge",
    "ble-probe": "pnpm --filter @buddy/bridge ble-probe"
  }
}
