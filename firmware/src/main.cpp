// Placeholder — replaced in Task 3 with the real hardware adapter.
// Guarded so it never interferes with the native test build.
#ifndef UNIT_TEST
#include <Arduino.h>
void setup() {}
void loop() {}
#endif
